---
trigger: always_on
---

This is an Information Retrieval project, that wants to use the scraped data in ./crawler to then index it and create a search engine frontend with apache solr and then automatically be deployed with the workflow on github to a vps server. We use yarn here.